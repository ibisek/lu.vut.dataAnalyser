def _max(a, b):
    if not a:
        return b
    if not b:
        return a
    return max(a, b)


def _min(a, b):
    if not a:
        return b
    if not b:
        return a
    return min(a, b)

